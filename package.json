{
  "name": "consultant-scheduler",
  "version": "1.0.0",
  "description": "Sistema de gerenciamento de agenda de consultores",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "mongo:dev": "mkdir -p .mongo-data && mongod --dbpath .mongo-data --bind_ip 127.0.0.1 --port 27017",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm run start --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}


